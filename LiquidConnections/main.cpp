#include "ui/ui.h"

int main(int argc, char **argv)
{
	LiquidConnections::UI::init(argc, argv);
	//findCudaGLDevice(argc, (const char**)argv);

	//createTexture();
	//createPixelBuffer();
	//createDataBuffer();

	//glutMainLoop();
	//timerEvent(0);
}